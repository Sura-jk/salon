import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.luxesalon.app',
  appName: 'LuxeSalon',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;